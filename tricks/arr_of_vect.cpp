#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<vector<int>> v(2);

    v[0].push_back(1);
    v[0].push_back(2);
    v[0].push_back(3);
    v[1].push_back(4);
    v[1].push_back(5);
    v[1].push_back(6);
    cout << v[0][2];
    // int n = 550;
    // int *a  = new int[n];

    return 0;
}