#include <bits/stdc++.h>
using namespace std;
int main()
{
    // Primitive Type
    // data types given as keywords known to compiler
    // This conversion is possible because both are primitive
    // Automatic type conversion in c++
    int x = 4;
    float y = x;
    char a = 'c';
    string s = "name";
    s = s + a;
    cout << s;
}